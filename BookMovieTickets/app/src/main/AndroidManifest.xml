<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hoang.bookmovietickets" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MovieDetails"
            android:label="@string/title_activity_movie_details"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".BookTicketsActivity"
            android:theme="@style/MyAppDialog" >
        </activity>
        <activity
            android:name=".MyListTicketsActivity"
            android:label="@string/title_activity_my_list_tickets"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".LoginForm"
            android:label="@string/title_activity_login_form"
            android:parentActivityName=".MainActivity">
        </activity>
        <activity
            android:name=".RegisterForm"
            android:label="@string/title_activity_register_form"
            android:parentActivityName=".LoginForm">
            // tam the da =)) :v,a``` con```, remove thoi de sau cung dc, 1

        </activity>
    </application>

</manifest>
